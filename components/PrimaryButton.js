import { Text, View } from 'react-native'

function PrimaryButton({btntext}) {
    return (
            <View>
                <Text>{btntext}</Text>
            </View>
    )
}

export default PrimaryButton;
